{
  "AssemblyConfig": {
    "AssemblyPath": "AssemblyReflector.Sample.dll",
    /*
      if true -> you already referenced the assembly in AssemblyReflector.Library
      if false -> you will provide in AssemblyPath the absolute path to the assembly .dll
    */
    "IsAssemblyIncluded": "true"
  }
}